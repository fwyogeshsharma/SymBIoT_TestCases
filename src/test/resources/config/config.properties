# ============================================
# SymBIoT Test Automation - Global Configuration
# ============================================

# Application URL (default environment)
base.url=https://symbiot.faberwork.com/

# Browser Configuration
browser=chromium
# Options: chromium, firefox, webkit

# Execution Mode
headless=false
slow.motion=0

# Viewport Settings
viewport.width=1920
viewport.height=1080

# Timeouts (in milliseconds)
default.timeout=30000
navigation.timeout=60000
action.timeout=10000

# Retry Configuration
retry.count=1
retry.delay=1000

# Screenshot & Video Settings
screenshot.on.failure=true
video.on.failure=true
trace.on.failure=true

# Test Data
default.username=clarehayden7@gmail.com
default.password=Faber321!

# Parallel Execution
parallel.threads=3

# Report Settings
report.directory=target/reports
allure.results.directory=target/allure-results